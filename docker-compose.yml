services:

  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://amishakapoor24_db_user:ZmjzDQ9scSuGIxwY@cluster0.jhfgemj.mongodb.net/?appName=wishlistdb
      - JWT_SECRET=mysupersecretkey123
    
  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:5000
    depends_on:
      - backend
      